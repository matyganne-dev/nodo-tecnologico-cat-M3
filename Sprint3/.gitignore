# Dependencias (lo más importante)
node_modules/

# Archivos de entorno (nunca subir .env)
.env
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema operativo (basura que no querés en Git)
.DS_Store
Thumbs.db